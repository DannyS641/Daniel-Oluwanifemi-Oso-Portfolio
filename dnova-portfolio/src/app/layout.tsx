import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Daniel Oluwanifemi Oso - Design Wizard",
  description:
    "Daniel Oluwanifemi Oso is a design wizard specializing in UI/UX design, web development, and brand strategy.",
  openGraph: {
    title: "Daniel Oluwanifemi Oso - Design Wizard",
    description:
      "Professional portfolio showcasing design and development work",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
